#include <iostream>

using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);

    long n;
    cin >> n;

    long result = n * 4;
    cout << result << "\n";
    
    return 0;
}
