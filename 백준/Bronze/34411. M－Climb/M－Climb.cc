#include <iostream>

using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int m, n, y;
    cin >> m >> n >> y;

    cout << m * n << "\n";

    return 0;
}
